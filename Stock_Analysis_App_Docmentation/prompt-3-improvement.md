# Prompt 1: Add AI-Powered Food Recognition

Enable plan mode and then run the following prompt: (Shift+Tab)

Copy and paste the following into Claude Code:

```
Context: I have a working Stock Analysis & Trade Tracking app with hardcoded user and Stock (Master Data) data and a local database. Now I want to make it Master Data as Stock, and user (id,name, email, password, mobile,Role, creationdTime, updatedtime) make user role table also (RoleId,RoleName), stock table (stockid, name) 

Instruction: Build the backend API and front end also, and also enable swagger Ui for all API so we can test from swagar also
change the db type from PostgreSQL to Mysql i have make db Schima in mySql as "stock_analysis"
DB Detail:
DB_USER = "root"
DB_PASSWORD = "root"
DB_HOST = "localhost"
DB_PORT = "3306"
DB_NAME = "stock_analysis"
Updated CLAUDE.md and a new dated entry in docs/ with what changed and why.
```

